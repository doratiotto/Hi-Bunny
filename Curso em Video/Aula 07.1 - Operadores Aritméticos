# Operadores Aritméticos (Contas

#                                  		Ordem
# ( )                               	*
# Adição            +               	****
# Subtração         -               	****
# Multiplicação     *               	***
# Divisão           /               	***
# Potência          **   pow(x,y)   	**
# Divisão Inteira:  //              	***
# Resto da Divisão  %               	***
# Igual             ==
# Raiz n			**(1/n)	 sqrt(x)	**

# Ordem de operação:  ( )  **
n1 = int(input('Digite um valor: '))
n2 = int(input('Digite outro valor: '))

s = n1 + n2
m = n1 * n2
d = n1 / n2
p = n1 ** n2
di = n1 //n2


print('A soma é {}, \n a multiplicação é {} \n e a divisão é {:.5f}' .format(s,m,d), end=', na mesma linha, ') 

# :.5 ou 0.5f diz as casas decimais do número e f é float
# end='' faz não quebrar a linja e escrece o que está dentro em string
# /n quebra de linha 

print('Divisão inteira: {1}, Potência: {0}' .format(p,di)) # os numeros {1} e {0} indicam a ordem das variáveis aparecendo
